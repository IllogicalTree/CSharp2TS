namespace CSharp2TS.CLI {
    public class Consts {
        // consts instead of enum so they can easily be specified in the config file
        public const string CamelCase = "camel";
        public const string PascalCase = "pascal";
    }
}
